{
  "name": "demo-nodejs-server",
  "version": "0.0.1",
  "private": true,
  "main": "app/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "dimmsd",
  "license": "ISC",
  "description": "Simple demo node server",
  "dependencies": {
  }
}
